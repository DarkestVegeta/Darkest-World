# Archive surface validation

GO269 adds regression coverage around the shared archive/navigation contract and its visual telemetry surface.

The tests intentionally keep navigation semantics in `DarkestWorldNavigationState` and presentation derivation in `DarkestWorldArchiveTelemetry`.
